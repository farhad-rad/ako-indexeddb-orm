export class Model {
    constructor(modelName, columns, options) {
        
    }
    /**
     * Compares two model instances
     * @param {Model} left 
     * @param {Model} right 
     * @returns {boolean}
     */
    static __equals(left, right){

    }
}